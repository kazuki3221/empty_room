<%= form_with(model: room2, local: true) do |form| %>
  <% if room2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room2.errors.count, "error") %> prohibited this room2 from being saved:</h2>

      <ul>
        <% room2.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<form>
  <div class="form-group">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>
  <div class="field">
    <%= form.label :floor %>
    <%= form.number_field :floor %>
  </div>

  <div class="field">
    <%= form.label :room_type %>
    <%= form.text_field :room_type %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
</form>
<% end %>
